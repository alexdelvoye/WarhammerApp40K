import { StyleSheet } from "react-native";

export const HomeScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#111",
  },

  header: {
    color: "white",
    fontSize: 36,
    fontWeight: "bold",
    marginTop: 20,
    marginHorizontal: 20,
  },

  list: {
    padding: 20,
  },
});
