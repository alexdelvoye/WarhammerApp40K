import { StyleSheet } from "react-native";

export const SelectUnitScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#111",
        padding: 20,
    },
});